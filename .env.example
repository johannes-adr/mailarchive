# copy to .env; the passwords referenced by "pass_env" in config.json live here
WEBDE_PASS=secret
# an account without pass_cmd/pass_env/pass falls back to MAILARCHIVE_PASS_<NAME>,
# e.g. the account "web.de":
# MAILARCHIVE_PASS_WEB_DE=secret
